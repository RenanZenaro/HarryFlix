@model List<Movie>
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">HarryFlix</h1>
</div>

@foreach (var movie in Model)
{
    <p>@movie.Title - @movie.OriginalTitle</p>
}